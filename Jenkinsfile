pipeline {
    agent any
    stages {
        stage('Verificar Webhook') {
            steps {
                echo '¡Webhook exitoso! El pipeline se ejecutó automáticamente.'
                checkout scm
            }
        }
    }
}
